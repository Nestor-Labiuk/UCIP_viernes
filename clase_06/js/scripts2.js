let giorni = ["Lunedì" , "Martedì", "Mercoledì", "Giovedì", "Venerdì", "Sabato", "Domenica"];

// let aa = giorni.join(' ');

// let bb = "UNO, DOS, TRES";

// let cc = bb.split(",");

// giorni.sort();

// console.log(giorni.length);

// or ||
// and &&

let num = [1,3,5,12,8,4];
let multiplo = 2;
num.forEach(function(e,i) {
    if(e > 4 ){
        console.log(e);
    }else{
        console.log(e*2);
    }
});

